<p id="notice"><%= notice %></p>

<%= cl_image_tag("jhfdtff947vmla3ffeaf",
      width: 400, height: 300, crop: :fill) %>

<%= cl_image_tag("mpnpcbwdteztzibxqhvk",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>

<p><%= cl_image_path("mpnpcbwdteztzibxqhvk") %></p>

<h1>Articles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>



<div class="banner" style="background-image: url(<%= cl_image_path "image_id" %>)">

</div>

<img src="

https://rubygems.org/image.png
